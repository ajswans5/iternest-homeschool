# IterNest Assets

Use this folder for repo-level visual assets: logos, icons, brand references, marketing images, and design exports.

Assets that are imported directly by the React app should live in `app/src/assets/` instead.
