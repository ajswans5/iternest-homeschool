# IterNest Prototypes

Use this folder for exploratory HTML prototypes and UI experiments.

Prototype code can be valuable reference material, but it should stay separate from the production React application until we intentionally migrate it.
